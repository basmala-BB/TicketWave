@model TicketWave.ViewModel.LoginVM

@{
    ViewData["Title"] = "Login";
}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card login-card shadow-lg p-4">
                <h2 class="text-center mb-4">Login</h2>

                <form asp-action="Login">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                    <div class="mb-3">
                        <label asp-for="UserNameOrEmail" class="form-label"></label>
                        <input asp-for="UserNameOrEmail" class="form-control" />
                        <span asp-validation-for="UserNameOrEmail" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Password" class="form-label"></label>
                        <input asp-for="Password" class="form-control" type="password" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <div class="form-check mb-3">
                        <input class="form-check-input" asp-for="RemeberMe" />
                        <label class="form-check-label" asp-for="RemeberMe"></label>
                    </div>

                    <div class="d-grid mb-3">
                        <button type="submit" class="btn btn-primary btn-lg">Login</button>
                    </div>

                    <div class="text-center">
                        <a asp-action="Register" class="text-decoration-none me-3">Register</a>
                        <a asp-action="ForgetPassword" class="text-decoration-none me-3">Forget Password</a>
                        <a asp-action="ResendEmailConfirmation" class="text-decoration-none">Resend Email</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<style>
    body {
        background: linear-gradient(to right, #111, #333);
        font-family: 'Segoe UI', sans-serif;
    }

    .login-card {
        background: rgba(0,0,0,0.65);
        border-radius: 15px;
        color: #fff;
        backdrop-filter: blur(6px);
        transition: transform 0.3s, box-shadow 0.3s;
    }

        .login-card:hover {
            transform: scale(1.02);
            box-shadow: 0 15px 30px rgba(0,0,0,0.6);
        }

    .form-control {
        background: rgba(255,255,255,0.05);
        border: 1px solid #555;
        color: #fff;
        transition: all 0.3s;
    }

        .form-control:focus {
            background: rgba(255,255,255,0.1);
            border-color: #0d6efd;
            box-shadow: none;
            color: #fff;
        }

    .btn-primary {
        background-color: #0d6efd;
        border-color: #0d6efd;
        transition: background-color 0.3s, transform 0.2s;
    }

        .btn-primary:hover {
            background-color: #0b5ed7;
            transform: scale(1.02);
        }

    a {
        color: #0d6efd;
        transition: color 0.2s;
    }

        a:hover {
            color: #0b5ed7;
        }
</style>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
